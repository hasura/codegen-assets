{
  "name": "scratchpad",
  "version": "1.0.2",
  "main": "dist/app.js",
  "ts:main": "src/index.ts",
  "typings": "src/index.ts",
  "license": "MIT",
  "@comment build": [
    "Webpack needs commonjs module set in tsconfig, but commonjs produces (slightly) larger bundles than ESNEXT.",
    "So instead of maintaing one tsconfig for webpack, and one for the actual project code,",
    "you can override ts-node configuration in-command to temporarily set module just to load webpack config."
  ],
  "scripts": {
    "build": "TS_NODE_COMPILER_OPTIONS='{\"module\":\"commonjs\"}' yarn webpack --config webpack.config.ts",
    "debug": "cd src && nodemon debug.ts",
    "serve-assets": "cd ../../ && npx sirv-cli --cors"
  },
  "dependencies": {
    "common-tags": "^1.8.0",
    "graphql": "^15.0.0",
    "graphql-extra": "0.3.0-alpha.3"
  },
  "devDependencies": {
    "@types/node": "^13.9.0",
    "@types/webpack": "^4.41.12",
    "glob": "^7.1.6",
    "nodemon": "^2.0.3",
    "ts-loader": "^7.0.2",
    "ts-node": "^8.9.0",
    "typescript": "^3.8.3",
    "webpack": "^5.0.0-beta.16",
    "webpack-cli": "^3.3.11"
  }
}
